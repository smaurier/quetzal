'use client';

// Éditeur d un jeu de cartes. Implémenté à la tâche 37 : ce placeholder
// existe uniquement pour que le manifeste client (tâche 36) pointe vers un
// module réel et que le typecheck reste vert entre les deux tâches.
export default function DeckEditor(_props: Readonly<Record<string, string>>) {
  return null;
}
